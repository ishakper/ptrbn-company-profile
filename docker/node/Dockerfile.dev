FROM node:20-alpine

WORKDIR /app

# Copy package descriptors first to leverage Docker build cache layers
COPY package*.json ./

RUN npm install

# Expose Vite HMR dev port
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
